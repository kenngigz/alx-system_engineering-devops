#!/bin/bash

# Read lines from input and extract the first letter
while read -r line; do
    first_letter="${line:0:1}"
    decoded_message="${decoded_message}${first_letter}"
done

# Print the decoded message with a new line at the end
echo "$decoded_message"

